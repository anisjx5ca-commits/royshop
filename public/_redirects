/* Netlify redirects for React Router SPA
   This ensures all routes are handled by index.html
   which then lets React Router handle the routing */
/*  /index.html  200
/api/*  https://your-api.com/:splat  200
